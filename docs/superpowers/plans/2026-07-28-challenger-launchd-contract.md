# v0.32 Challenger LaunchAgent 合同实施计划

1. 冻结 macOS、Asia/Shanghai、固定 4h cadence 与无凭据边界。
2. 实现 plist/合同构建、自哈希、Schema 和语义重放。
3. 实现 owner-only runtime/output 发布与只渲染 CLI。
4. 覆盖路径、时区、参数、权限、幂等、冲突和篡改测试。
5. 生成 not-installed 证据，更新 ADR、README、版本与 Evaluator build。
6. 运行 focused/full validation，提交、合并并标记 `v0.32.0`。
