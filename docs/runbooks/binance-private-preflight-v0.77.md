# Binance private preflight v0.77

This release is code-complete candidate material, not activation authority.
Fail closed unless the released build, disabled configuration, egress IP,
owner-only credential capability, account approval, stage activation, venue
permissions, one-way isolated mode, leverage cap and flat account all replay
exactly.

If any check differs, make no private request and submit no order. Preserve the
exact error and immutable inputs; do not delete evidence. Never guess account
state or edit an approval to make a check pass.
